import 'package:flutter/material.dart';

class calendarioPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text('Horario escolar'),),);
  }
}